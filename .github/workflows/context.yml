
name: CI

on:
  push:

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

      - name: Context
        env:
          COMMIT_ID: ${{github.sha}}
        run: echo "Commit id => $COMMIT_ID"
    
