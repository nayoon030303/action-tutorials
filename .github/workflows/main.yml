

name: Hello world

on:
 
  push:
    branches:
      - main 
  
  workflow_dispatch:

jobs:
  build:
  
    runs-on: ubuntu-latest
   
    steps:
      - name: Run pwd
        run: pwd 

      - name: Run ls -al
        run: ls -al
